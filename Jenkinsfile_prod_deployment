def remote = [:]
remote.name = "stage"
remote.host = "192.168.245.128"
remote.allowAnyHosts = true
withCredentials([usernamePassword(credentialsId: 'fe95e991-94b8-4103-a575-7c6c6bf277f9', passwordVariable: 'password', usernameVariable: 'userName')]) {
        remote.user = userName
        remote.password = password
}
pipeline {
  agent {label 'Slave1'}
  stages {
  
  stage("Interactive_Input") {
            steps {
                script {

                    // Variables for input
                    def inputChange

                    // Get the input
                    def userInput = input(
                            id: 'userInput', message: 'Enter Change Number',
                            parameters: [
                                    string(defaultValue: '',
                                            name: 'Change')
                            ])

                    // Save to variables. Default to empty string if not found.
                    inputChange = userInput.Change?:''

                    // Echo to console
                    echo("Change Number: ${inputChange}")
                }
            }
        }
          
        
          
    stage('Test') {
      parallel {
        stage('Maven') {
          steps {
            echo 'Running from Jenkins file'
            bat(script: 'mvn compile', label: 'maven')
          }
        }

        stage('Cucumber Report') {
          steps {
            cucumber '**/*.json'
          }
        }

      }
    }     

  }
        post('Send Mail') {
        always {
            emailext body: 'A Test EMail', recipientProviders: [[$class: 'DevelopersRecipientProvider'], [$class: 'RequesterRecipientProvider']], subject: 'Test'
        }
    }
}
